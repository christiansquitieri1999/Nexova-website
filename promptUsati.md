#PROMPT

##1

Create into index.htmo semantic HTML5 strcture, i will run locally with npx. I need an header section with company and logo of Nexova. The hero section represnt the company (HR recruiting services putting human skills and huamn being in each interview process). Add into hero section after the value proposition (UVP) 3 main why or benefit section and 3 core feature 1 experience. After hero, implement a footer with contact information.

I want always tailwind when is possible os write directly inside index.html, i need mobile first implementation and so dynamically adaptive/responsive across devices and resizing of screen (I want tailwind breakpoints sm md lg), ARIA attributes where needed to improved accessability.

##2

The side core service put the 3 point in a prisma image to nto add just another box into write text. Remove the box regarding experience and just put some real experience some story from person you get hired and other from he firm who hired someone, finally in footer remove "hours"

Include a link/utton that directs to application form, add images from unspalsh regarding right context and aligned with nexova brand and add a descriptive alt tags to all images now added (just 1 or maximum 2)

##3

why you put the part of expereince in applicaton keep into inde.html or tell me if i'm worng

##4

Use semantic HTML tags (section, article, nav etc), add schema.org markpu for company information like business type or organization.

Finally in the photo attached how nexova looks like, change the font to be more realist, remove initial button "human-centered recruiting" and see again the brand identity with logo and how is written Nexova, in addition second photo to see a model reference

##5

It's already created the application form. Read the context.md and include all specific fields requirred by using proper input types like date number and text. Add <label> correctly associated with each input using for attribute, group related fields using <fieldset> and <legend> whete appropriate. Mark reuirred fields with the required attributed

##6

Since you already write code into validation.js, implement real time validation (as the user types or on blur), display specific error messages for each type of validation in order to help the user to fullfill with right value and therefore style error messages clearly and visibly. All these seattlements are putted in place to prevent form submission if there are validation errors, finally show a success message when validation is correct (simulate submission)

##7

add a button to clear the form. Implement responsive form design and tailwind spaing, sizes e focus states